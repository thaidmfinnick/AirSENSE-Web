<!DOCTYPE html>
<html> 
<% include ../Layout/header.ejs %>
<head>
    <script src="https://code.jquery.com/jquery.min.js"></script>
    <script src="/js/mqtt.min.js"></script>
    <style>
        #all-comment {
            margin-top: 60px;
        }
        .col- {
        float: left;
        margin: 0 5px;
        font-weight: 400;
        font-size: 12px;
        opacity: 0.75;
        }
        .upload-file-container {
            display: none;
        }
/**/
        
    </style>
</head>
    <body class="defult-home">
		<!-- Main content Start -->
        <% include ../Layout/topbar.ejs %>

<div class="comment">
    <p>This is the comment, OK????</p>
    <p>This is the comment, OK????</p>
    <p>This is the comment, OK????</p>
    <p>This is the comment, OK????</p>
    <p>This is the comment, OK????</p>
    <p>This is the comment, OK????</p>
    <div class="content-playlist-wrap"> 
        <div class="comment-content-wrapper">
            <h3 class="comment-co playlist-section-listntent-heading">Bình luận</h3>
            <div class="my-comment-wrapper">
                <div id="avatar-user"></div>
                <input type="text" class="my-comment-box" placeholder="Viết bình luận của bạn..." id="contentComment" required>
                <form action="/api/document/uploadFile" class='upload-file-container' method="POST" enctype="multipart/form-data">
                    <input type="file" class="form-control" id="uploadFile" name="uploadFile" />
                    <label for="uploadFile">Attack a file</label>
                    <button type="submit">Submit</button>
                </form>
                <div class="comment-action-wrapper">
                    <button id="attack-btn" class="comment-cancel-btn">Đính kèm tệp</button>
                    <button class="comment-cancel-btn">Huỷ</button>
                    <button class="comment-submit-btn" onclick="comment()">Bình luận</button>
                </div>
            </div>
                <div id="all-comment"></div>
            </div>
        </div>
</div>
<script>
    const myCommentBox = document.querySelector('.my-comment-box');
    const commentActionWrapper = document.querySelector('.comment-action-wrapper');

    myCommentBox.oninput = () => {
        if (!!myCommentBox.value) {
            commentActionWrapper.style = 'display:flex';
        } else {
            commentActionWrapper.style = 'display:none';
        }
    };
    const sectionTitleWrapper = document.querySelectorAll('.section-title-wrapper');
    const learnList = document.querySelectorAll('.learn-list');
    const sectionTitleIcon = document.querySelectorAll('.section-title-icon-up');

    sectionTitleWrapper.forEach((item, index) => {
        sectionTitleWrapper[index].onclick = () => {
            learnList[index].classList.toggle('learn-list--hidden');
            sectionTitleIcon[index].classList.toggle(
                'section-title-icon-up--rotate180deg'
            );
        };
    });
    var check = true;
    const uploadFile = document.querySelector('.upload-file-container');
    const attackBtn = document.getElementById('attack-btn');
    attackBtn.onclick = () => {
        if(check) {
            check = !check;
            uploadFile.style = 'display: block';
        }
        else {
            check = !check;
            uploadFile.style = 'display: none';
        }
    }


</script>

        
          
        <!-- Main content End -->
        <% include ../Layout/footer.ejs %>

    </body>
    <script>
        
         const HOST_HTTP_COMMENT = 'http://127.0.0.1:3002/api/comment';
         const HOST_HTTP_HISTORY_COMMENT = 'http://127.0.0.1:3002/api/history';
        // fake data user

        const token = '';
        getInfoUser();
        function getInfoUser() {
            var Link = '/api/auth/user';
            $.ajax({    type: 'get',
                        method: 'GET',
                        url: Link,
                        data: {}, // body data type must match "Content-Type" header
                        cache: 'no-cache', // *default, no-cache, reload, force-cache, only-if-cached
                        credentials: 'same-origin', // include, *same-origin, omit
                        redirect: 'follow', // manual, *follow, error
                        referrerPolicy: 'no-referrer', // no-referrer, *no-referrer-when-downgrade, origin, origin-when-cross-origin, same-origin, strict-origin, strict-origin-when-cross-origin, unsafe-url
                        dataType: 'json',
                        cache: 'no-cache', // *default, no-cache, reload, force-cache, only-if-cached
                        credentials: 'same-origin', // include, *same-origin, omit
                        headers: {
                            'Content-Type': 'application/json',
                            'Accept': 'application/json',
                            'Content-Type': 'application/json',
                            'authorization': 'Beard ' + token,
                            // 'Authorization': 'Beard ' + localStorage.getItem('LVC_tocken'),

                            // 'Content-Type': 'application/x-www-form-urlencoded',
                        },
                success: function (loadcofig) {
                    console.log('loadcofig: ',loadcofig);
                    let htmlUser = ` 
                    <img src="${loadcofig.user.avartar}" alt="" class="my-avatar" />
                    <img src='https://graph.facebook.com/516400012839921/picture?width=400&height=400' alt='' class='my-avatar' />
                    <h5>${loadcofig.user.name}</h5>
                    `
                    $('#avatar-user').html(htmlUser);

                },
                error: function (loadcofig) {
                    console.log('error');
                    console.log(loadcofig);
                }
            });
        }

        // upload file
        function upload(e) {
            e.preventDefault();
                alert('OK');
            
        }


        // Xác định đang comment cho bài học nào
        // ....
        // Code sau khi lên khóa học, bây giờ dummy ra



        // chạy mqtt


        const clientId = 'mqttjs_' + Math.random().toString(16).substr(2, 8)
        const host = 'ws://localhost:3002';
        var infoData="";

        const options = {
        keepalive: 60,
        clientId: clientId,
        port:3002,
        username : "test",
        useNewUrlParser: true,
        password: "testadmin",
        protocolId: 'MQTT',
        protocolVersion: 4,
        clean: true,
        reconnectPeriod: 1000,
        connectTimeout: 30 * 1000,
            will: {
                topic: '#',
                payload: 'Connection Closed abnormally..!',
                qos: 0,
                retain: false
            },
        }

        console.log('Connecting mqtt client')
        const client = mqtt.connect(host, options);

        client.on('connect', () => {
            console.log('Client connected:' + clientId)
            // Subscribe
            client.subscribe('#', { qos: 0 })
        });
        // Received
        client.on('message', (topic, message, packet) => {
            console.log('Received Message: ' + message.toString() + '\nOn topic: ' + topic);
            infoData +="/n \n"+message.toString();
            $("#infoMessage").html(infoData); 
        });

        client.on('error', (err) => {
            console.log('Connection error: ', err)
            client.end()
        });

        client.on('reconnect', () => {
            console.log('Reconnecting...')
        });

        // get info user comment
        async function getInfoComment(id) {
            let user = null;
            var linkurl = '/api/auth/getInfo';
            await $.ajax({
                type: 'post',
                        method: 'POST',
                        url: linkurl,
                        data: JSON.stringify( {
                            userid: id
                        }), // body data type must match "Content-Type" header
                        cache: 'no-cache', // *default, no-cache, reload, force-cache, only-if-cached
                        credentials: 'same-origin', // include, *same-origin, omit
                        redirect: 'follow', // manual, *follow, error
                        referrerPolicy: 'no-referrer', // no-referrer, *no-referrer-when-downgrade, origin, origin-when-cross-origin, same-origin, strict-origin, strict-origin-when-cross-origin, unsafe-url
                        dataType: 'json',
                        cache: 'no-cache', // *default, no-cache, reload, force-cache, only-if-cached
                        credentials: 'same-origin', // include, *same-origin, omit
                        headers: {
                            'Content-Type': 'application/json',
                            'Accept': 'application/json',
                            'Content-Type': 'application/json',
                            'authorization': 'Beard ' + token,
                            // 'Authorization': 'Beard ' + localStorage.getItem('LVC_tocken'),

                            // 'Content-Type': 'application/x-www-form-urlencoded',
                        },
                        success: function (loadcofig) {
                            user = loadcofig
                        },
                        error: function (error) {
                            console.log('error ...', error)
                        }
            })
            return user;
        }

        // get content all comment
        getAllComment();
        function getAllComment () {
            var Link =HOST_HTTP_HISTORY_COMMENT + '/content';
            $.ajax({    type: 'post',
                        method: 'POST',
                        url: Link,
                        data: JSON.stringify( {
                            chatRoom: '/chat/1'
                        }), // body data type must match "Content-Type" header
                        cache: 'no-cache', // *default, no-cache, reload, force-cache, only-if-cached
                        credentials: 'same-origin', // include, *same-origin, omit
                        redirect: 'follow', // manual, *follow, error
                        referrerPolicy: 'no-referrer', // no-referrer, *no-referrer-when-downgrade, origin, origin-when-cross-origin, same-origin, strict-origin, strict-origin-when-cross-origin, unsafe-url
                        dataType: 'json',
                        cache: 'no-cache', // *default, no-cache, reload, force-cache, only-if-cached
                        credentials: 'same-origin', // include, *same-origin, omit
                        headers: {
                            'Content-Type': 'application/json',
                            'Accept': 'application/json',
                            'Content-Type': 'application/json',
                            'authorization': 'Beard ' + token,
                            // 'Authorization': 'Beard ' + localStorage.getItem('LVC_tocken'),

                            // 'Content-Type': 'application/x-www-form-urlencoded',
                        },
                success:  function (loadcofig) {
                    var dataAllComments = loadcofig.chatRoom; 
                    dataAllComments.map(async (item)=> {
                        console.log('user', item);
                        var d = new Date(item.time * 1000);
                        var date = d.getDate() + '/' + (d.getMonth() + 1) + '/' + d.getFullYear() + ' ' + d.getHours() + ':' + d.getMinutes();
                        console.log('time', date);
                        let userComment = await getInfoComment(item.content.author_id);
                        if(userComment.user.avartar!=='') {
                        let commentItem = `<div class="other-comment-item">
                            <img src='${userComment.user.avartar}' alt="" class="other-commnet-avatar">
                    <div class="other-comment-body">
                        <div class="other-comment-box">
                            <h5 class="other-commnet-author playlist-section-list">${userComment.user.name}</h5>
                            <p class="other-comment-content">${item.content.content}</p>
                        </div>
                        <div class='sub-comment-item'>
                        <p class='sub-comment-like col-'>Like</p>
                        <p class='sub-comment-reply col-'>Reply</P>
                        <p class='time-comment col-'>${date}</p>
                        </div>
                    </div>
                </div>`
                $('#all-comment').append(commentItem);
            }
            else {
                let commentItem = `<div class="other-comment-item">
                            <img src='https://graph.facebook.com/516400012839921/picture?width=400&height=400' alt="" class="other-commnet-avatar">
                    <div class="other-comment-body">
                        <div class="other-comment-box">
                            <h5 class="other-commnet-author playlist-section-list">${userComment.user.name}</h5>
                            <p class="other-comment-content">${item.content.content}</p>
                        </div>
                        <div class='sub-comment-item'>
                        <p class='sub-comment-like col-'>Like</p>
                        <p class='sub-comment-reply col-'>Reply</P>
                        <p class='time-comment col-'>${date}</p>
                        </div>

                    </div>
                </div>`
                $('#all-comment').append(commentItem);
            }

                    })
                },
                error: function (error) {
                    console.log('error ...', error)
                }
        })
        
        }

        // comment
        function comment () {
            let linkUploadFile = '';
            if(!!!$('#uploadFile').val()) {
                // save to upload file for comment folder
                
                // fix linkUploadFile again
            }


            var Link =HOST_HTTP_COMMENT + '/chat';
            $.ajax({    type: 'post',
                        method: 'POST',
                        url: Link,
                        data: JSON.stringify( {
                            post_id: 1,
                            author_IP: '',
                            content: $('#contentComment').val(),
                            coment_tag: '',
                            comment_atack: '',
                            comment_parent_id: 2
                        }), // body data type must match "Content-Type" header
                        cache: 'no-cache', // *default, no-cache, reload, force-cache, only-if-cached
                        credentials: 'same-origin', // include, *same-origin, omit
                        redirect: 'follow', // manual, *follow, error
                        referrerPolicy: 'no-referrer', // no-referrer, *no-referrer-when-downgrade, origin, origin-when-cross-origin, same-origin, strict-origin, strict-origin-when-cross-origin, unsafe-url
                        dataType: 'json',
                        cache: 'no-cache', // *default, no-cache, reload, force-cache, only-if-cached
                        credentials: 'same-origin', // include, *same-origin, omit
                        headers: {
                            'Content-Type': 'application/json',
                            'Accept': 'application/json',
                            'Content-Type': 'application/json',
                            'authorization': 'Beard ' + token,
                            // 'Authorization': 'Beard ' + localStorage.getItem('LVC_tocken'),

                            // 'Content-Type': 'application/x-www-form-urlencoded',
                        },
                success: function (loadcofig) {
                    console.log('loadcofig: ',loadcofig);
                },
                error: function (error) {
                    console.log('error ...', error)
                }
        })
    }



        
        





         
    </script>
</html>